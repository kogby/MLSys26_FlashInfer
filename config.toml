[solution]
name = "JensenHireMe-solution-v2"      # Solution name
# Switch between tracks by toggling the `definition` and `entry_point` pair below.
# definition = "dsa_sparse_attention_h16_ckv512_kpe64_topk2048_ps64"   # sparse attention (kernel_v2.py)
definition = "dsa_topk_indexer_fp8_h64_d128_topk2048_ps64"             # topk indexer   (kernel_topk_indexer.py)
author = "JensenHireMe"              # Team/author name

[build]
language = "triton"               # triton | cuda
# entry_point = "kernel_v2.py::run"                 # paired with sparse_attention definition above
entry_point = "kernel_topk_indexer.py::run"         # our solution (Level 2a: Triton fused score kernel)
